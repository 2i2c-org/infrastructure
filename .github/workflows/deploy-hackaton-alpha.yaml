name: Deploy and Test hackathon-alpha hubs

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/deploy
        with:
          cluster: 'hackathon-alpha'