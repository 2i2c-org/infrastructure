{{ if .Values.gcp-filestore-backups.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: gcp-filestore-backups-sa
  annotations: {{ .Values.gcp-filestore-backups-sa.annotations | toJson }}
{{- end }}
