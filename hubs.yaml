hubs:
  - name: another-test
    domain: another-test.alpha.2i2c.cloud
    config: 
      jupyterhub:
        auth:
          type: google
  - name: test
    domain: test.alpha.2i2c.cloud
    config:
      jupyterhub:
        auth:
          type: github
        singleuser:
          memory:
            limit: 512M
  - name: spelman
    domain: spelman.alpha.2i2c.cloud
    config:
      jupyterhub:
        auth:
          type: google
        singleuser:
          memory:
            limit: 1G
