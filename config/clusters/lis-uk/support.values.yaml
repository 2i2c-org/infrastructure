prometheusIngressAuthSecret:
  enabled: true

prometheus:
  server:
    ingress:
      enabled: true
      hosts:
        - prometheus.ds.lis.2i2c.cloud
      tls:
        - secretName: prometheus-tls
          hosts:
            - prometheus.ds.lis.2i2c.cloud
grafana:
  ingress:
    hosts:
      - grafana.ds.lis.2i2c.cloud
    tls:
      - secretName: grafana-tls
        hosts:
          - grafana.ds.lis.2i2c.cloud
