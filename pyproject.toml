[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = [
    "./tests",
]
# You can use `D:` (default) as prefix if you don't want to override existing
# environment variables
env = [
    "D:RUN_ENV = test",
]
