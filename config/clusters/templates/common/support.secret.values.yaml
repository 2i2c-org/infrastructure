prometheus:
  server:
    probeHeaders:
      - name: Authorization
        value: Basic {{ encoded_pair }}
  serverFiles:
    web.yml:
      basic_auth_users:
    {{ username }}: {{ salted_password }}
prometheusAuthSecret:
  username: {{ username }}
  password: {{ password }}
