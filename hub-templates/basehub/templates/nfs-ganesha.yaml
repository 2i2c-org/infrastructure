{{ if .Values.nfsGanesha.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: home-nfs
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "nfs"
  resources:
    requests:
      storage: {{ .Values.nfsGanesha.size }}
{{ end }}
